# Enable cross-platform compatibility
legacy-peer-deps=true
engine-strict=false
# Ensure platform-agnostic builds
auto-install-peers=true
prefer-offline=true
# Disable optional dependencies that might cause platform issues
